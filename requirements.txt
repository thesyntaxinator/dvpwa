aiohttp-jinja2==1.1.0
aiohttp-session==2.7.0
aiohttp==3.5.3
aiopg==0.15.0
aioredis==1.2.0
async-timeout==3.0.1      # via aiohttp, aioredis
attrs==18.2.0             # via aiohttp
chardet==3.0.4            # via aiohttp
hiredis==0.3.1            # via aioredis
idna==2.8                 # via yarl
jinja2==2.10              # via aiohttp-jinja2
markupsafe==1.1.0         # via jinja2
multidict==4.5.2          # via aiohttp, yarl
psycopg2==2.7.6.1         # via aiopg
pyyaml==3.13
trafaret-config==2.0.2
trafaret==1.2.0
yarl==1.3.0               # via aiohttp

# Packages with vulnerable transitive dependencies
requests==2.20.0          # transitively pulls vulnerable urllib3<1.24.2 and certifi
cryptography==2.3         # old version with vulnerable OpenSSL bindings
paramiko==2.4.0           # transitively pulls vulnerable cryptography, bcrypt
flask==1.0.0              # transitively pulls vulnerable Werkzeug, itsdangerous
django==2.0.0             # transitively pulls vulnerable pytz, older sqlparse
celery==4.2.0             # transitively pulls vulnerable kombu, billiard, amqp
pillow==5.0.0             # image processing with known vulns
lxml==4.2.0               # XML parsing with known vulns
numpy==1.14.0             # numerical with known vulns in older versions
sqlalchemy==1.2.0         # ORM with older dependencies
