# Original dvpwa dependencies
aiohttp-jinja2==1.1.0
aiohttp-session==2.7.0
aiohttp==3.5.3
aiopg==0.15.0
aioredis==1.2.0
pyyaml==3.13
trafaret-config==2.0.2
trafaret==1.2.0

# Packages with TRANSITIVE vulnerable dependencies

# requests 2.25.0 pulls vulnerable urllib3<1.26.5, certifi
requests==2.25.0

# cryptography 3.4.0 pulls vulnerable cffi
cryptography==3.4.0

# paramiko 2.7.0 pulls vulnerable cryptography, bcrypt
paramiko==2.7.0

# flask 1.1.0 pulls vulnerable Werkzeug<2.0, itsdangerous<2.0, Jinja2<3.0
flask==1.1.0

# pillow 8.0.0 has known CVEs
pillow==8.0.0

# lxml 4.6.0 has known CVEs in libxml2 bindings
lxml==4.6.0

# httpx 0.18.0 pulls vulnerable httpcore, h11
httpx==0.18.0

# Additional packages with known CVEs

# django 2.2.0 has multiple CVEs (SQLi, XSS, directory traversal)
django==2.2.0

# pyjwt 1.7.0 has CVE-2022-29217 (algorithm confusion)
pyjwt==1.7.0

# jinja2 2.10 has sandbox escape CVEs
jinja2==2.10

# urllib3 1.25.0 has CRLF injection and multiple CVEs
urllib3==1.25.0

# setuptools 39.0.0 has RCE CVE
setuptools==39.0.0
